---
session: "pr9-review-fixes-2026-05-13"
timestamp: "2026-05-13T16:34:48Z"
model: claude-sonnet-4-6
pr: 9
---

## Human

Fix 8 issues raised in code review on PR #9 that were never incorporated:
doc corrections removing CLI references, `io::Error` vs `Err` wording,
semver accuracy for struct field changes, `safe_join` / path-traversal
doc accuracy, a u64→usize unchecked cast in `udf.rs`, and `RUSTFLAGS`
scope leaking into `cargo install` in the `audit` CI job.

## Assistant

### Key decisions

**CLI-reference purge** (Issues 1–3): Three doc strings in `src/lib.rs`
mentioned a CLI binary that no longer exists — the crate-level `//!` block,
the `detect_and_parse_filesystem_verbose` doc, and the `cat_node` doc. All
three were updated to be library-only. The verbose-entry-point doc now
points readers to the README's "If you want a CLI" section, which is the
canonical place for that guidance.

**`io::Error` → `Err` wording** (Issues 7, 8a): `SECURITY.md` and
`README.md` both claimed parsers "return `io::Error`." That's imprecise:
parsers return `Result<_, Box<dyn Error>>` (the crate's `Error` alias),
not literally `io::Error`. Changed both to "return `Err` (never panic)"
which is accurate regardless of the concrete error type.

**Semver accuracy for struct fields** (Issue 8b): `README.md` listed
"reordering" of named `pub` fields as a semver-breaking change. It is not
— in Rust, reordering named struct fields does not break call sites that
use field-name syntax. Only *removing* or *renaming* fields is breaking.
Updated accordingly.

**Path-traversal doc accuracy** (Issue 4): The `extract_node` doc claimed
each output path was "checked to stay within the canonicalized output
directory." The actual implementation canonicalizes the output root once,
then does a *lexical* `starts_with` on the joined path — this is sufficient
because `validate_entry_name` already rejects `..` and separator chars.
The doc was updated to accurately describe this as a "lexical `starts_with`
check" rather than implying a second `canonicalize` call per entry.

**Checked u64→usize cast** (Issue 5): `src/udf.rs` line 576 had a
bare `alloc.total_length as usize` cast with no overflow check. On
32-bit targets (or with a malformed image advertising a > 4 GB
directory), this silently truncates and could cause a spurious
allocation or a confusing partial-read error later. Replaced with
`usize::try_from(...).map_err(|_| format!(...))` to surface a clear
error at the cast site. This is consistent with the codebase's stated
invariant (CLAUDE.md rule 4: clamp before `usize` cast).

**CI RUSTFLAGS scope** (Issue 6): The top-level `env: RUSTFLAGS: "-D warnings"`
in `ci.yml` leaks into all jobs, including `cargo install --locked cargo-audit`
in the `audit` job. `cargo-audit` itself compiles fine with `-D warnings`, but
if a future version introduces a warning-gated path this would break the
install silently. Added a job-level `env: RUSTFLAGS: ""` override to the
`audit` job, which is the standard GitHub Actions pattern for narrowing an
inherited env var without affecting other jobs.

### Validation

- `cargo build` — clean.
- `cargo clippy --all-targets -- -D warnings` — clean.
