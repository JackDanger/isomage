---
session: "coverage-zip"
timestamp: "2026-05-14T16:47:14Z"
model: claude-sonnet-4-6
---

## Human

Make sure all of our work is in PRs and/or merged and that there's fantastic tests with 100% coverage over everything. Release new versions as appropriate.

## Assistant

### Key decisions

**zip.rs coverage** (94.68%/95.67% → 95.95% regions / 97.48% lines): Added 8 tests covering error paths and edge cases.

**`From<io::Error> for Error`** (lines 87-89):
- `error_from_io_error` — direct `Error::from(io::Error)` call

**`find_eocd` edge cases**:
- `find_eocd_signature_too_close_to_end` — EOCD sig at position 18 in 22-byte buffer → `eocd.len()=4 < 22` → NotZip (line 119)
- `find_eocd_zip64_too_close_to_start` — EOCD at offset 0 with zip64 sentinel → `eocd_abs=0 < EOCD64_LOCATOR_SIZE=20` → NotZip (line 131)
- `find_eocd_zip64_wrong_locator_sig` — zip64 EOCD at offset 20+; bytes before it have wrong locator sig (all zeros) → NotZip (line 138)
- `find_eocd_zip64_wrong_eocd64_sig` — locator matches `EOCD64_LOCATOR_SIG` but EOCD64 block has wrong sig (all zeros) → NotZip (line 145)

**`parse_central_directory` paths**:
- `cd_entry_with_extra_field_calls_parse_zip64_extra` — CD entry with `extra_len > 0` and `uncompressed_size=0xFFFFFFFF` (sentinel) → `parse_zip64_extra` called (lines 205-209); uncompressed size correctly resolved from extra field
- `cd_break_on_non_cdr_sig` — CD buffer has 4 bytes of zeros appended after the valid CDR entry; at next iteration `sig != CDR_SIG` → break (line 176)

**`parse_zip64_extra` truncated size**:
- `zip64_extra_size_overflow_breaks` — tag 0x0001 claims size=100 but only 8 bytes of data → `pos + size > extra.len()` → break (line 239); all sentinel values remain unchanged

**What I skipped**: Lines 403-422 (`make_crc32_table` const fn). LLVM coverage instruments runtime execution only; `const fn` is evaluated at compile time and never appears in the runtime coverage bitmap. These lines are unreachable by any test.
