---
session: "coverage-multi-format"
timestamp: "2026-05-14T18:00:00Z"
model: claude-sonnet-4-6
---

## Human

Make sure all of our work is in PRs and/or merged and that there's fantastic tests with 100% coverage over everything. Release new versions as appropriate.

## Assistant

### Key decisions

Added 2 tests to `src/formats/tar.rs` to cover previously uncovered lines.

**`tar.rs`** (tests added for `Error::from(io::Error)` and `scan_entries` NotTar path):

- `error_from_io_error` — covers the `From<io::Error>` impl at lines 84-86: constructs an `io::Error` and calls `Error::from()` directly, asserting `Error::Io(_)`.

- `scan_entries_non_ustar_first_block_returns_not_tar` — covers the `return Err(Error::NotTar)` branch at line 193. Called `scan_entries` directly (bypassing `detect`) with a 512-byte all-0xFF first block followed by two 512-byte zero EOF blocks. The first block is non-zero (so not EOF) but has no `ustar` magic at offset 257, causing `scan_entries` to return `Error::NotTar` after processing the first entry.

**What remains uncoverable**:
- Line 179: `Err(e) => return Err(Error::Io(e))` — non-EOF IO error arm; `Cursor<Vec<u8>>` never returns non-EOF errors.
- Lines 218, 233: LLVM closing-brace artifacts for `if leftover > 0` and `if ustar_magic_ok` blocks — genuinely unreachable instrumentation points.
