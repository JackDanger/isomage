---
session: "coverage-ext"
timestamp: "2026-05-14T16:41:48Z"
model: claude-sonnet-4-6
---

## Human

Make sure all of our work is in PRs and/or merged and that there's fantastic tests with 100% coverage over everything. Release new versions as appropriate.

## Assistant

### Key decisions

**ext.rs coverage** (89.60%/91.66% → 98.04% regions / 99.25% lines): Added 8 tests covering previously untested code paths.

**`read_ptr_block` + single-indirect directory** (lines 469-483, 518-525):
- `make_ext2_single_indirect_dir` helper: root dir inode with i_block[0..12]=0 and i_block[12]=SI_BLK; the SI pointer block at block 8 points to the actual directory data block 6.
- `single_indirect_dir_parsed_correctly` — exercises `read_ptr_block` and the SI branch of `collect_classical_blocks`

**Extent-based directory reading** (lines 631-640):
- `make_ext4_extent_dir` helper: builds on `make_ext2_single_indirect_dir`, sets EXT4_EXTENTS_FL on the root dir inode and replaces its i_block with an extent tree pointing to DIR_DATA_BLK.
- `extent_dir_read_entries_extent_path` — root dir uses extents → `read_dir_entries` takes the extent branch

**`scan_dir_block` bad rec_len** (line 603):
- `scan_dir_block_bad_rec_len_breaks` — entry with rec_len=4 < 8 → immediate break; called directly since it's in the same module

**`single_run_location` first block zero** (line 709):
- `single_run_location_first_block_zero_returns_none` — regular file with i_block[0]=0 → first=0 → Ok(None)

**Block device inode skipped** (lines 767-768):
- `block_device_inode_skipped_in_tree` — S_IFBLK inode in directory; build_tree returns Ok(None) for it (neither is_dir nor is_reg nor is_symlink)

**Double-indirect blocks** (lines 532-549, 551-553):
- `classical_blocks_double_indirect` — direct call with: 12 direct blocks (covered=12288), SI block all zeros (no contribution), DI-L1 at block 14 → L2 at block 15 → data at block 16; size=12289 forces DI traversal

**Triple-indirect blocks** (lines 555-578, 580):
- `classical_blocks_triple_indirect` — direct call with 12 direct blocks, SI=0, DI=0, TI-L1 at block 17 → L2 at 18 → L3 at 19 → data at block 20

**`build_tree` depth limit** (line 732):
- `build_tree_exceeds_max_depth_returns_none` — direct call with depth=MAX_DEPTH+1 → returns Ok(None)

**What I skipped**: Line 161 (IO error in superblock read — not UnexpectedEof; not testable with `Cursor<Vec<u8>>`). Lines 538, 566, 578 (LLVM closing-brace artifacts for `break` targets in DI/TI outer loops that require an l1ptr=0 after valid l1ptrs — complex to trigger without massive test data). Lines 690, 692, 714 (LLVM closing-brace artifacts). Lines 782, 793, 797 (error paths in `detect_inner` requiring non-EOF IO errors).
