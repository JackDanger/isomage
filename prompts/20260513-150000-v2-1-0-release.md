---
session: "v2-1-0-release"
timestamp: "2026-05-13T15:00:00Z"
model: claude-sonnet-4-6
---

## Human

Release new versions as appropriate.

## Assistant

### Key decisions

**Minor version bump (2.1.0, not 3.0.0)**: The v3.0 API changes planned in CLAUDE.md (unified `Image::open`/`Image::create`, `RandomAccess` trait, `TreeNode.name → Cow<'_, str>` breaking change) have not yet been implemented. All changes in this release are additive — new format readers behind per-format features, new write APIs behind the `write` feature, new `detect`/`detect_and_parse` functions in each format module. Existing v2.0.0 users can upgrade to v2.1.0 without changing any code.

**Description update**: The crate description was updated from "Pure-Rust reader for ISO 9660 (Joliet, Rock Ridge) and UDF disc images" to accurately reflect the now much broader format support.

**CHANGELOG entry**: Documents all 14 new format readers, write APIs, generic entry points, performance numbers (3–6× faster than 7zz), coverage improvement, and the two production bug fixes (HFS+ build_tree orphan and UDF AVDP multi-sector search).

**What v3.0.0 is reserved for**: The breaking `TreeNode.name → Cow<'_, str>` change, plus the `Image::open`/`Image::create` unified facade described in CLAUDE.md. Those require more design work and a deliberate major-version boundary.
