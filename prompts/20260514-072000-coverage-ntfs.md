---
session: "coverage-ntfs"
timestamp: "2026-05-14T07:20:00Z"
model: claude-sonnet-4-6
---

## Human

Make sure all of our work is in PRs and/or merged and that there's fantastic tests with 100% coverage over everything. Release new versions as appropriate.

## Assistant

### Key decisions

**ntfs.rs coverage** (91.80% → 98.54% regions, 91.36% → 99.15% lines): Added 41 tests covering all previously untested paths:

**`apply_fixup` edge cases**:
- `apply_fixup_usa_offset_overflow` — usa_offset + usa_count*2 > buf.len() → return false (combined OR condition)
- `apply_fixup_usn_mismatch_continues` — sector-end bytes differ from USN → mismatch body (line 196) is entered; fixup non-fatal, continues

**`read_mft_record` via detect_and_parse**:
- `read_mft_record_bad_fixup_returns_none` — slot 6 with usa_count=1 → apply_fixup false → Ok(None) (line 227)
- `read_mft_record_not_in_use_returns_none` — slot 6 with good fixup but flags bit 0=0 → Ok(None) (line 233)

**`parse_attributes` None paths**:
- `parse_attributes_resident_data_end_overflow` — value_length overflows attr bounds → resident_data=None (line 291)
- `parse_attributes_resident_too_short_for_header` — buf.len()=45, pos+24=46 > 45 → inner else (line 294)
- `parse_attributes_nonresident_slice_overflow` — non_resident=false → nonresident_slice=None (line 303 else)

**`decode_runlist` paths**:
- `decode_runlist_truncated_run_breaks` — header 0x11 + only 1 byte (no offset) → break (line 408)
- `decode_runlist_sparse_run` — header 0x10 (off_size=0) → had_sparse=true (lines 440-442)

**`namespace_priority` all arms**:
- `namespace_priority_all_values` — asserts NS_WIN32_DOS=3, NS_WIN32=2, NS_POSIX=1, NS_DOS=0, unknown=0 (lines 597-605)

**`build_tree_recursive` direct calls**:
- `build_tree_recursive_too_deep` — depth=MAX_DEPTH+1 → TooDeep (line 617)
- `build_tree_recursive_with_file_children` — root with file_location=Some and file_location=None children (lines 640-649)
- `build_tree_recursive_with_subdirectory` — root with dir child containing grandchild (lines 631-637)
- `build_tree_recursive_too_deep_child_skipped` — dir child at depth=MAX_DEPTH causes TooDeep → silently skipped (lines 634-636)
- `build_tree_recursive_orphan_node` — mft_num not in map and ≠ROOT → empty directory (line 659)

**Non-resident $DATA via detect_and_parse**:
- `nonresident_data_single_run_file_location` — non-resident attr with single-run runlist → file_location=Some(LCN*cluster_size) (lines 549-565)
- `nonresident_data_nr_slice_too_short_no_location` — nr_attr_len=56 < 64 → inner if FALSE → file_location=None (line 567)
- `nonresident_data_runlist_offset_oob_no_location` — runlist_offset=72 == nr_slice.len() → not < → file_location=None (line 566)

**`extract_record_info` direct calls** (via `make_eri_buf` helper):
- `extract_record_info_standard_information_in_record` — $STANDARD_INFORMATION attr → match arm body (lines 571-573)
- `extract_record_info_attribute_list_in_record` — $ATTRIBUTE_LIST attr → same arm
- `extract_record_info_unknown_attr_type_catch_all` — type 0x60 → `_ => {}` (line 575)
- `extract_record_info_dos_namespace_skipped` — $FILE_NAME with NS_DOS(2) → attr_pos+=; continue (lines 518-519)
- `extract_record_info_two_filename_attrs_namespace_wins` — two $FILE_NAME: NS_WIN32 then NS_WIN32_DOS → `Some(existing)` branch comparison (lines 523-525); higher-priority name wins
- `extract_record_info_attr_length_zero_breaks_inner_loop` — documents that line 579 is synchronized with parse_attributes and can't diverge
- `extract_record_info_filename_attr_parse_returns_none` — $FILE_NAME value_length=10 < 66 → parse_filename_attr → None → `if let Some(fn_attr)` FALSE (line 531)
- `extract_record_info_filename_attr_resident_data_none` — $FILE_NAME value_length=200 overflows attr → resident_data=None → `if let Some(data)` FALSE (line 532)
- `extract_record_info_data_attr_resident_data_none_no_location` — $DATA resident with overflowing value_length → resident_data=None → `if let Some(data)` FALSE (line 548)

**`detect_and_parse` loop behavior**:
- `detect_and_parse_skips_self_referencing_record` — user record with parent_ref=mft_num → children_map loop `continue` (line 748)

**What I skipped**: Lines 218, 673, 682, 703 (IO error paths requiring non-EOF errors from non-Cursor readers). Lines 503, 507, 579 (inner-loop defensive guards synchronized with parse_attributes, unreachable independently). Lines 568, 637, 730 (effectively dead code: 568 requires non_resident=true with nonresident_slice=None which can't occur without breaking; 637 requires a non-TooDeep error from a function that can only return TooDeep or Ok; 730 requires mft_num==5 in the mft_num>=12 branch, which is impossible).
