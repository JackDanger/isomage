---
session: "coverage-udf"
timestamp: "2026-05-13T17:00:00Z"
model: claude-sonnet-4-6
---

## Human

Make sure all of our work is in PRs and/or merged and that there's fantastic tests with 100% coverage over everything.

## Assistant

### Key decisions

**udf.rs coverage uplift (76.5% → 86.2%)**: Added 14 new tests covering previously-uncovered branches in the UDF parser.

**FSD nearby scan** (`parse_udf_fsd_found_via_nearby_scan`, `parse_udf_fsd_nearby_scan_verbose`): Two new image variants where the primary FSD sector has tag_id=0 (not a valid FSD) so the parser scans sectors 1..32 nearby. One image has the FSD at offset+1; `parse_udf_no_fsd_returns_err` covers the case where no FSD is found in the entire scan window.

**Extent-based file** (`parse_udf_extent_based_file_has_location`): Built a synthetic image where a file FE uses short allocation descriptors (ad_type=0) rather than inline data. This exercises the `alloc.extents.first() → Some(first)` branch in `parse_directory` that emits `TreeNode::new_file_with_location`. Verified that `file_location` is set to the correct byte offset.

**Deleted FID** (`parse_udf_deleted_fid_skipped`, `parse_udf_deleted_fid_verbose`): Image with FID buffer containing a zero-tag entry (tag_id=0 → advance 4 bytes), a parent FID, a deleted file FID (file_characteristics & 0x04), and a valid file FID. Confirms deleted entries are skipped and don't appear in the tree.

**Bad file entry fallback** (`parse_udf_bad_file_entry_emits_zero_size_node`, `parse_udf_bad_file_entry_verbose`): FID whose ICB points to a sector with tag_id=0 (invalid). `get_file_info` returns Err → `parse_directory` emits a zero-size TreeNode for the file. Verified the fallback node appears with size=0.

**get_file_allocation edge cases**:
- `file_alloc_empty_inline_data_returns_err`: ad_type=3 with ad_length=0 → no inline_data written → Err
- `file_alloc_short_ad_next_extent_type_breaks`: extent_type=3 (next-extent-of-ADs) → early break → Err
- `file_alloc_long_ad_next_extent_type_breaks`: same for long ADs (ad_type=1)
- `file_alloc_long_ad_sparse_skipped`: long AD with extent_type=1 (sparse) → skipped → Err

**Overall TOTAL coverage: ~87.2%** (udf.rs improved from 76.5% to 86.2% region coverage).
